<?xml version="1.0"?>
<project name="AP2DX" default="main" basedir=".">
	<!-- Sets variables which can later be used. -->
	<!-- The value of a property is accessed via ${} -->
	<property name="ant.dir" location="ant" />
	<property name="src.dir" location="src" />
	<property name="build.dir" location="${ant.dir}/build" />
	<property name="javadoc.dir" location="${ant.dir}/javadoc" />

	<!-- Variables used for JUnit testin -->
	<property name="test.report.dir" location="${ant.dir}/testreport" />

	<!-- Define the classpath which includes the junit.jar and the classes after compiling-->
    <path id="AP2DX.classpath">
        <pathelement location="bin"/>
        <pathelement location="lib/json_simple-1.1.jar"/>
        <pathelement location="lib/junit-4.9b2.jar"/>
    </path>


	<!-- Deletes the existing build, docs and dist directory-->
	<target name="clean">
		<delete dir="${ant.dir}" />
	</target>

	<!-- Creates the  build, docs and dist directory-->
	<target name="makedir">
		<mkdir dir="${build.dir}" />
		<mkdir dir="${javadoc.dir}" />
		<mkdir dir="${test.report.dir}" />
	</target>

	<!-- Compiles the java code (including the usage of library for JUnit -->
	<target name="compile" depends="clean, makedir">
		<javac srcdir="${src.dir}" destdir="${build.dir}"
			fork="true" memoryMaximumSize="384m" memoryInitialSize="128m">
			<classpath refid="AP2DX.classpath" />
		</javac>

	</target>

	<!-- Run the JUnit Tests -->
	<!-- Output is XML, could also be plain-->
	<target name="junit" depends="compile">
		<junit printsummary="on" fork="true" maxmemory="256M" haltonfailure="no">
			<classpath refid="AP2DX.classpath" />
			<formatter type="xml" />
			<batchtest todir="${test.report.dir}">
				<fileset dir="${src.dir}">
					<include name="**/*Test*.java" />
				</fileset>
			</batchtest>
		</junit>
	</target>

	<target name="javadoc">
	    <javadoc
	           access="public"
	           destdir="${javadoc.dir}"
	           author="true"
	           version="true"
	           use="true"
	       >

	        <fileset dir="${src.dir}" defaultexcludes="yes">
	            <include name="**/*.java"/>
	            <exclude name="AP2DX/test/**/*.java" />
	        </fileset>
	    </javadoc>
	</target>
	
	<target name="main" depends="compile, junit, javadoc">
		<description>Main target</description>
	</target>

</project>